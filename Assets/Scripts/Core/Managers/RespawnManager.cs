using UnityEngine;

namespace Player
{
    public class RespawnManager : MonoBehaviour
    {
        // Singleton
        // Track all respawn triggers in a scene
        // Track last respawn trigger
        // Handle Scene Transition
        //  If the player enters a scene, there should be at least one respawn trigger and spawn the player there.
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
